name: yaml_api_dart
description: A Dart REST API using YAML instead of JSON for all responses.
version: 1.0.0
license: BSD-2-Clause
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.7.2

# Add regular dependencies here.
dependencies:
  yaml_writer: ^2.1.0
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  mysql_client: ^0.0.27
  yaml: ^3.1.3
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
